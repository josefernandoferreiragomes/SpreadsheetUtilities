# Copilot Instructions

## Project Guidelines
- The user prefers clean, working solutions. When Blazor form binding issues occur with FormName, use RegisterRequest DTO directly (not nested classes). Use OnSubmit not OnValidSubmit with FormName. Keep components simple and straightforward.